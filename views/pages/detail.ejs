<!DOCTYPE html>
<html>

<head>
  <title>Book Details</title>
</head>

<body>
  <h1>Book Details</h1>
  <main>
    <header></header>
    <div></div>
    <div>
      <ul>
          <li>
            <div>
              <img src="<%= book.image %>" alt="<%= book.title %>"">
            </div>
            <h3><%= book.title %></h3>
            <h4><%= book.authors %></h4>
            <p><%= book.summary %></p>
            <p><%= book.isbn %></p>
            <p><%= book.bookshelf %></p>
          </li>
      </ul>
    </div>
    <footer></footer>
  </main>
</body>

</html>

